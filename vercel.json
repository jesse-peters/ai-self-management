{
  "buildCommand": "pnpm --filter '@projectflow/db' build && pnpm --filter '@projectflow/core' build && pnpm --filter '@projectflow/web' build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "cleanUrls": true
}
